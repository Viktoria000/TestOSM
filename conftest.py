from fixtures import object_class_server
import logging_config
import logging
log_server = logging.getLogger("server")
log_test_direct = logging.getLogger("direct")
log_test_revers = logging.getLogger("revers")